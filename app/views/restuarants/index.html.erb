<div class="jumbotron">
  <h2> YelpDemo is the best way to find food and shit on local restuaraunts. </h2>
  <h3> Explore the favorite diners pastry shops and eateries in your community.  <br/>
  Leave reviews and ratings for your fellow foodies. </h3>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <% if user_signed_in? && current_user.admin? %>

      <th colspan="2"></th>

      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @restuarants.each do |restuarant| %>
      <tr>
        <td><%= link_to restuarant.name, restuarant %></td>
        <td><%= restuarant.address %></td>
        <td><%= restuarant.phone %></td>
        <td><%= link_to "Restuarant Website", restuarant.website %></td>
        <% if user_signed_in? && current_user.admin? %>

        <td><%= link_to 'Edit', edit_restuarant_path(restuarant), class: "btn btn-link"  %></td>
        <td><%= link_to 'Destroy', restuarant, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link"  %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if user_signed_in? && current_user.admin? %>
<%= link_to 'New Restuarant', new_restuarant_path, class: "btn btn-link"  %>
<% end %>

<%= image_tag "https://s3-us-west-1.amazonaws.com/yelpdemoa/happy_footer.png" %>
